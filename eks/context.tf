terraform {
    required_version = ">= 1.5.0, < 2.0.0"

  # backend "s3" {
  #   bucket  = "xpoz-tf" # Bucket for TF states
  #   key     = "state/dev" # Path inside TF states bucket
  #   region  = "eu-west-1"
  # }
}